on: [push]

name: CI

jobs:
  linux:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@master
      - name: add
        shell: bash
        uses: docker://linuxbrew/brew
        run: |
          brew tap .
          brew update
      - name: install
        shell: bash
        uses: docker://linuxbrew/brew
        run: |
          brew install git-workspace
  macos:
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@master
      - name: add
        shell: bash
        run: |
          brew tap .
          brew update
      - name: install
        shell: bash
        run: |
          brew install git-workspace
  audit:
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@master
      - name: Audit
        shell: bash
        run: |
          brew audit --strict *.rb
      - name: Update
        shell: bash
        run: |
          brew tap local/local file://$(pwd)
      - name: Install
        shell: bash
        run: |
          brew install git-workspace
